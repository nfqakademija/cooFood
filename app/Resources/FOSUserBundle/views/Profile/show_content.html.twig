{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
    <p>{{ 'Vardas'}}: {{ user.name }}</p>
    <p>{{ 'Pavardė'}}: {{ user.surname }}</p>
    <button type="button" class="btn btn-primary" onClick="location.href='../profile/edit'">Redaguoti</button>
    <button type="button" class="btn btn-default" onClick="location.href='{{ 'homepage' }}'">Atgal</button>
</div>
