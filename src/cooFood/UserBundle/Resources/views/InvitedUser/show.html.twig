{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InvitedUser</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Idevent</th>
                <td>{{ entity.idEvent }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Secretcode</th>
                <td>{{ entity.secretCode }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('inviteduser') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('inviteduser_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
