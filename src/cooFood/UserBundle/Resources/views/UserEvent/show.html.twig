{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserEvent</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ entity.idUser }}</td>
            </tr>
            <tr>
                <th>Idevent</th>
                <td>{{ entity.idEvent }}</td>
            </tr>
            <tr>
                <th>Paid</th>
                <td>{{ entity.paid }}</td>
            </tr>
            <tr>
                <th>Accepteduser</th>
                <td>{{ entity.acceptedUser }}</td>
            </tr>
            <tr>
                <th>Acceptedhost</th>
                <td>{{ entity.acceptedHost }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('userevent') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('userevent_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
