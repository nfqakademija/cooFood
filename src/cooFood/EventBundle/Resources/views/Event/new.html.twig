{% extends '::base.html.twig' %}

{# Next line is to include form field override #}
{% form_theme form ':Form:fields.html.twig' %}

{% block body %}
    <div class="row create-event">
        <div class="col-md-6 col-md-offset-3">
            <legend>{% trans %}Naujas renginys{% endtrans %}</legend>
            {{ form_start(form) }}
            <div class="form-group new-event">
                <div class="col-md-12">
            {{ form_widget(form) }}
                </div>
            </div>
            <div class="form-group new-event">
                <div class="col-md-6">
                    <input class="btn btn-success" type="submit" value="{% trans %}Sukurti{% endtrans %}" />
                </div>
                <div class="col-md-6">
                    <a type="button" class="btn btn-danger pull-right" href="{{ path('homepage') }}">{% trans %}Atgal{% endtrans %}</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
